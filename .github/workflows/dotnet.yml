name: .NET

on: [push]
jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - name: welcome
        run: echo "WELCOME"
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'
      - run: echo aaaa
